{
  "name": "testbank-async-demo",
  "install": "npm --prefix backend install && npm --prefix frontend install",
  "terminals": [
    {
      "name": "api",
      "command": "npm --prefix backend run dev",
      "description": "Test Bank API. Transfers, bill payments, quota. Listens on port 3001."
    },
    {
      "name": "web",
      "command": "npm --prefix frontend run dev -- --host",
      "description": "Test Bank web client. Port 5173, proxies /api to port 3001."
    }
  ],
  "ports": [
    { "name": "api", "port": 3001 },
    { "name": "web", "port": 5173 }
  ]
}
